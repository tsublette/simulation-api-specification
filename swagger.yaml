swagger: "2.0"
info:
  version: 1.1.0
  title: Simulation API
  description: An API for managing simulations within 3DSIM.
  termsOfService: https://3dsim.com
  contact:
    name: 3DSIM API Team
    email: support@3dsim.com
    url: https://3dsim.github.io
  license:
    name: MIT
    url: http://github.com/gruntjs/grunt/blob/master/LICENSE-MIT
host: 3dsim.cloud.tyk.io
basePath: /simulation-api
schemes:
  - https
consumes:
  - application/json
produces:
  - application/json
paths:
  /status:
    get:
      description: |
        Use this endpoint to check server status.
      operationId: status
      responses:
        "200":
          description: status response
          schema:
            $ref: '#/definitions/Status'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'

  /materials:
    get:
      description: Get list of materials
      operationId: getMaterials
      parameters:
        - name: organizationId
          in: query
          description: the organization id to get items for.  Must be provided as API callers only have access to items
            belonging to their organization.
          required: true
          type: integer
          format: int32
        - name: offset
          in: query
          description: starting paging count; ex. 60 will skip the first 60 items in the list
          required: false
          type: integer
          format: int32
          default: 0
        - name: limit
          in: query
          description: number of items to return within the query
          required: false
          type: integer
          format: int32
          default: 10
        - name: sort
          in: query
          description: key:direction pairs for one or multiple field sort orders.  e.g. sort=key1:desc,key2:asc
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
        - name: archived
          in: query
          description: If true, will only return archived items.  If false, will only return unarchived items.  If not
            provided, will return both archived and unarchived items.
          required: false
          type: boolean
      responses:
        "200":
          description: Successfully retrieved list
          headers:
            X-Pagination:
              type: string
              description: Contains paging information in json format - totalCount, totalPages
          schema:
            type: array
            items:
              $ref: '#/definitions/Material'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:materials
    post:
      description: Creates a new material for the organizationId specified
      operationId: addMaterial
      parameters:
        - name: material
          in: body
          description: Material to add
          required: true
          schema:
            $ref: '#/definitions/Material'
      responses:
        "200":
          description: material response
          schema:
            $ref: '#/definitions/Material'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:materials

  /materials/{id}:
    get:
      description: Returns a material based on a single ID
      operationId: getMaterial
      parameters:
        - name: id
          in: path
          description: ID of material to fetch
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: material response
          schema:
            $ref: '#/definitions/Material'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Material not found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:materials

    put:
      description: Update a material
      operationId: updateMaterial
      parameters:
        - name: id
          in: path
          description: material identifier
          required: true
          type: integer
          format: int32
        - name: material
          in: body
          required: true
          schema:
            $ref: '#/definitions/Material'
      responses:
        "200":
          description: Material was successfully updated.
          schema:
            $ref: '#/definitions/Material'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Material not found (id invalid)
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:materials

  /materials/{id}/archive:
    put:
      description: archives a single material based on the ID supplied
      operationId: archiveMaterial
      parameters:
        - name: id
          in: path
          description: ID of material to archive
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: material that has been archived with active field cleared
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Material not found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:materials

  /machines:
    get:
      description: Get list of machines
      operationId: getMachines
      parameters:
        - name: organizationId
          in: query
          description: the organization id to get items for.  Must be provided as API callers only have access to items
            belonging to their organization.
          required: true
          type: integer
          format: int32
        - name: offset
          in: query
          description: starting paging count; ex. 60 will skip the first 60 items in the list
          required: false
          type: integer
          format: int32
          default: 0
        - name: limit
          in: query
          description: number of items to return within the query
          required: false
          type: integer
          format: int32
          default: 10
        - name: sort
          in: query
          description: key:direction pairs for one or multiple field sort orders.  e.g. sort=key1:desc,key2:asc
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
        - name: archived
          in: query
          description: If true, will only return archived parts.  If false, will only return unarchived parts.  If not
            provided, will return both archived and unarchived parts.
          required: false
          type: boolean
      responses:
        "200":
          description: Successfully retrieved list of machines
          headers:
            X-Pagination:
              type: string
              description: Contains paging information in json format - totalCount, totalPages
          schema:
            type: array
            items:
              $ref: '#/definitions/Machine'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:machines

    post:
      description: Add a machine
      operationId: postMachine
      parameters:
      - name: machine
        in: body
        required: true
        schema:
          $ref: '#/definitions/Machine'
      responses:
        "200":
          description: Machine was successfully added.
          schema:
            $ref: '#/definitions/Machine'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:machines

  /machines/{id}:
    get:
      description: Get a machine
      operationId: getMachine
      parameters:
      - name: id
        in: path
        description: machine identifier
        required: true
        type: integer
        format: int32
      responses:
        "200":
          description: Successfully retrieved the requested machine
          schema:
            $ref: '#/definitions/Machine'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Machine not found (id invalid)
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:machines

    put:
      description: Update a machine
      operationId: putMachine
      parameters:
        - name: id
          in: path
          description: machine identifier
          required: true
          type: integer
          format: int32
        - name: machine
          in: body
          required: true
          schema:
            $ref: '#/definitions/Machine'
      responses:
        "200":
          description: Machine was successfully updated.
          schema:
            $ref: '#/definitions/Machine'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Machine not found (id invalid)
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:machines

  /machines/{id}/archive:
    put:
      description: Archive a machine, making it not available for use in a simulation (sets archived property to true)
      operationId: archiveMachine
      parameters:
      - name: id
        in: path
        description: machine identifier
        required: true
        type: integer
        format: int32
      responses:
        "200":
          description: Machine was successfully archived.
          schema:
            $ref: '#/definitions/Machine'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Machine not found (id invalid)
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:machines

  /machines/{id}/unarchive:
    put:
      description: Unarchive a machine, making it available for use in a simulation (sets archived property to false)
      operationId: unarchiveMachine
      parameters:
      - name: id
        in: path
        description: machine identifier
        required: true
        type: integer
        format: int32
      responses:
        "200":
          description: Machine was successfully unarchived.
          schema:
            $ref: '#/definitions/Machine'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Machine not found (id invalid)
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:machines

  /parts:
    get:
      description: Get list of parts
      operationId: getParts
      parameters:
        - name: organizationId
          in: query
          description: the organization id to get items for.  Must be provided as API callers only have access to items
            belonging to their organization.
          required: true
          type: integer
          format: int32
        - name: offset
          in: query
          description: starting paging count; ex. 60 will skip the first 60 items in the list
          required: false
          type: integer
          format: int32
          default: 0
        - name: limit
          in: query
          description: number of items to return within the query
          required: false
          type: integer
          format: int32
          default: 10
        - name: sort
          in: query
          description: key:direction pairs for one or multiple field sort orders.  e.g. sort=key1:desc,key2:asc
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
        - name: availability
          in: query
          description: If specified, will filter to only include parts with the given availability.  Uploaded - the part has been uploaded.  Processing - the part
            is being processed.  Available - the part was processed successfully and can be used in simulations.  Error -
            an error occurred, contact support@3dsim.com.
          required: false
          type: string
          enum:
            - Uploaded
            - Processing
            - Available
            - Error
        - name: archived
          in: query
          description: If true, will only return archived parts.  If false, will only return unarchived parts.  If not
            provided, will return both archived and unarchived parts.
          required: false
          type: boolean
      responses:
        "200":
          description: Successfully found the list of parts
          headers:
            X-Pagination:
              type: string
              description: Contains paging information in json format - totalCount, totalPages
          schema:
            type: array
            items:
              $ref: '#/definitions/Part'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:parts
    post:
      description: Adds a part and starts processing the part.  When processing is finished, the "availability" field
        for the part will change to "Available".  See GET /parts.
      operationId: addParts
      parameters:
        - name: Part
          in: body
          description: Part to add. First, call parts/geometryurl to get a PartUploadRequest object.
            PUT the STL file AmazonS3 with the signed URL. Use the s3Key property as the value of fileLocation.
            A POST with this object will execute the part processing service.
          required: true
          schema:
            $ref: '#/definitions/PartPost'
      responses:
        "200":
          description: Successfully added a part
          schema:
            $ref: '#/definitions/Part'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:parts

  /parts/geometryuploadurl:
    get:
      description: Gets a time sensitive URL to upload a geometry file to AmazonS3.
      operationId: getNewPartGeometryUploadUrl
      responses:
        "200":
          description: Successfully returns a part geometry url for a new part upload
          schema:
            $ref: '#/definitions/PartUploadRequest'
        "403":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:parts

  /parts/{id}:
    get:
      description: Get a single part by Id
      operationId: getPart
      parameters:
        - name: id
          in: path
          description: ID of part to get
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully found a part
          schema:
            $ref: '#/definitions/Part'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Not found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:parts
    patch:
      description: Update fields within a part
      operationId: patchPart
      parameters:
        - name: id
          in: path
          description: ID of part to update
          required: true
          type: integer
          format: int32
        - name: partPatch
          in: body
          description: This endpoint uses JSON Patch, RFC 6092.
          required: true
          schema:
              type: array
              items:
                $ref: '#/definitions/PatchDocument'
      responses:
        "200":
          description: Patched part
          schema:
            $ref: '#/definitions/Part'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Part not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:parts
    delete:
      description: This will delete a part and all its associated geometry files
      operationId: DeletePart
      parameters:
        - name: id
          in: path
          description: ID of a part
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully deleted a part
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Not Found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:parts
    put:
      description: Updates a Part
      operationId: UpdatePart
      parameters:
        - name: id
          in: path
          description: ID of a part
          required: true
          type: integer
          format: int32
        - name: Part
          in: body
          description: Part to update.
          required: true
          schema:
            $ref: '#/definitions/Part'
      responses:
        "200":
          description: Successfully updated a part
          schema:
            $ref: '#/definitions/Part'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:parts

  /parts/{id}/archive:
    put:
      description: Archive a Part. Archiving a part will set the status to archived and will exclude the part from get list requests. To unarchive a part call /parts/{id}, change the status and then /parts (put) to update.
      operationId: ArchivePart
      parameters:
        - name: id
          in: path
          description: ID of part
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully archived a part
          schema:
            $ref: '#/definitions/Part'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Not Found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:parts

  /parts/{id}/geometryurl:
    get:
      description: Gets a time sensitive URL to the geometry file stored in AmazonS3.
      operationId: getPartGeometryUrl
      parameters:
        - name: id
          in: path
          description: ID of part
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully returns a part geometry url
          schema:
            $ref: '#/definitions/PartGeometryUrl'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Not Found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:parts

  /parts/{id}/geometry:
    get:
      description: Gets a list of triangles for the geometry file stored in AmazonS3.
      operationId: getPartGeometry
      parameters:
        - name: id
          in: path
          description: ID of part
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully returns a part geometry triangles
          schema:
            type: array
            items:
              $ref: '#/definitions/Triangle'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Not Found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:parts

  /parts/{partId}/supports/{supportId}:
    get:
      description: Get a single support by Id
      operationId: getPartSupport
      parameters:
        - name: partId
          in: path
          description: ID of part
          required: true
          type: integer
          format: int32
        - name: supportId
          in: path
          description: ID of support to get
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully found a support
          schema:
            $ref: '#/definitions/PartSupport'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Not found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:parts
    patch:
      description: Update fields within a support
      operationId: patchPartSupport
      parameters:
        - name: partId
          in: path
          description: ID of part
          required: true
          type: integer
          format: int32
        - name: supportId
          in: path
          description: ID of support to get
          required: true
          type: integer
          format: int32
        - name: partSupportPatch
          in: body
          description: This endpoint uses JSON Patch, RFC 6092.
          required: true
          schema:
              type: array
              items:
                $ref: '#/definitions/PatchDocument'
      responses:
        "200":
          description: Patched part support
          schema:
            $ref: '#/definitions/PartSupport'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Part not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:parts
    delete:
      description: This will delete a support and all its associated geometry files
      operationId: DeletePartSupport
      parameters:
        - name: partId
          in: path
          description: ID of part
          required: true
          type: integer
          format: int32
        - name: supportId
          in: path
          description: ID of support to get
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully deleted a part support
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Not Found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:parts
    put:
      description: Updates a Support
      operationId: updatePartSupport
      parameters:
        - name: partId
          in: path
          description: ID of part
          required: true
          type: integer
          format: int32
        - name: supportId
          in: path
          description: ID of support to get
          required: true
          type: integer
          format: int32
        - name: PartSupport
          in: body
          description: PartSupport to update.
          required: true
          schema:
            $ref: '#/definitions/PartSupport'
      responses:
        "200":
          description: Successfully updated a part support
          schema:
            $ref: '#/definitions/PartSupport'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:parts

  /parts/{partId}/supports:
    post:
      description: Adds a support and starts processing the support.  When processing is finished, the "availability" field
        for the part will change to "Available".
      operationId: postPartSupport
      parameters:
        - name: partId
          in: path
          description: ID of part
          required: true
          type: integer
          format: int32
        - name: PartPost
          in: body
          description: Support to add. First, call parts/:partId/supports/geometryurl to get a PartUploadRequest object.
            PUT the STL file AmazonS3 with the signed URL. Use the s3Key property as the value of fileLocation.
            A POST with this object will execute the part processing service.
          required: true
          schema:
            $ref: '#/definitions/PartSupportPost'
      responses:
        "200":
          description: Successfully added a part support
          schema:
            $ref: '#/definitions/PartSupport'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:parts

  /parts/{partId}/supports/geometryuploadurl:
    get:
      description: Gets a time sensitive URL to upload a support geometry file to AmazonS3.
      operationId: SupportGeometryUploadUrl
      parameters:
        - name: partId
          in: path
          description: ID of part
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully returns a part support geometry url for a new support upload
          schema:
            $ref: '#/definitions/PartUploadRequest'
        "403":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:parts

  /parts/{partId}/supports/{supportId}/geometryurl:
    get:
      description: Gets a time sensitive URL to the geometry file stored in AmazonS3.
      operationId: PartSupportGeometryUrl
      parameters:
        - name: partId
          in: path
          description: ID of part
          required: true
          type: integer
          format: int32
        - name: supportId
          in: path
          description: ID of support
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully returns a support geometry url
          schema:
            $ref: '#/definitions/PartGeometryUrl'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Not Found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:parts

  /parts/{partId}/supports/{supportId}/geometry:
    get:
      description: Gets a list of triangles for the geometry file stored in AmazonS3.
      operationId: PartSupportGeometry
      parameters:
        - name: partId
          in: path
          description: ID of part
          required: true
          type: integer
          format: int32
        - name: supportId
          in: path
          description: ID of support
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully returns support geometry triangles
          schema:
            type: array
            items:
              $ref: '#/definitions/Triangle'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Not Found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:parts

  /supports/{id}:
    get:
      description: Get a single support by Id
      operationId: getPartSupportById
      parameters:
        - name: id
          in: path
          description: ID of support to get
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully found a support
          schema:
            $ref: '#/definitions/PartSupport'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Not found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:parts

  /simulations:
    get:
      description: Gets a list of all simulations
      operationId: getSimulations
      parameters:
        - name: organizationId
          in: query
          description: the organization id to get items for.  Must be provided as API callers only have access to items
            belonging to their organization.
          required: true
          type: integer
          format: int32
        - name: status
          in: query
          description: simulation status for items retrieved.  If an array of items is sent, they are treated as "OR" operations.
            e.g. status=InProgress,Requested would yield a list of simulations that are in either state.
          required: false
          type: array
          items:
            type: string
            enum:
              - Draft
              - Requested
              - InProgress
              - Cancelled
              - Error
              - Success
        - name: offset
          in: query
          description: starting paging count; ex. offset of 60 will skip the first 60 items in the list
          required: false
          type: integer
          format: int32
          default: 0
        - name: limit
          in: query
          description: number of materials to return within the query
          required: false
          type: integer
          format: int32
          default: 10
        - name: sort
          in: query
          description: key:direction pairs for one or multiple field sort orders.  e.g. sort=key1:desc,key2:asc
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
        - name: archived
          in: query
          description: True to get only archived simulations, False to get only unarchived simulations, leave the parameter off to get all simulation types
          required: false
          type: boolean
        - name: partId
          in: query
          description: returns simulations using the specified part id
          required: false
          type: integer
          format: int32
        - name: supportId
          in: query
          description: returns simulations using the specified part support id
          required: false
          type: integer
          format: int32
        - name: buildFileId
          in: query
          description: returns simulations using the specified build file id
          required: false
          type: integer
          format: int32
        - name: isParent
          in: query
          description: returns either parent (true) or child (false) simulations
          required: false
          type: boolean
        - name: requiresLicense
          in: query
          description: returns simulations that require licence
          required: false
          type: boolean
      responses:
        "200":
          description: Successfully found the list of items
          headers:
            X-Pagination:
              type: string
              description: Contains paging information in json format - totalCount, totalPages
          schema:
            type: array
            items:
              $ref: '#/definitions/Simulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations

  /simulations/{id}:
    get:
      description: Get a simulation
      operationId: getSimulation
      parameters:
      - name: id
        in: path
        description: simulation identifier
        required: true
        type: integer
        format: int32
      responses:
        "200":
          description: Successfully retrieved simulation
          schema:
            $ref: '#/definitions/Simulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Simulation not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations
    patch:
      description: Patch a simulation
      operationId: patchSimulation
      parameters:
      - name: id
        in: path
        description: simulation identifier
        required: true
        type: integer
        format: int32
      - name: simulationPatch
        in: body
        description: This endpoint uses JSON Patch, RFC 6092.
        required: true
        schema:
            type: array
            items:
              $ref: '#/definitions/PatchDocument'
      responses:
        "200":
          description: Successfully patched simulation
          schema:
            $ref: '#/definitions/Simulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Simulation not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /simulations/{id}/cancel:
    put:
      description: Request a cancellation of a simulation.
      operationId: RequestCancellation
      parameters:
        - name: id
          in: path
          description: ID of part
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully requested cancellation
          schema:
            $ref: '#/definitions/Part'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Not Found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /simulations/{id}/cancelled:
    put:
      description: Confirms full cancellation of an active simulation.
      operationId: ConfirmCancellation
      parameters:
        - name: id
          in: path
          description: ID of part
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully set status to cancelled
          schema:
            $ref: '#/definitions/Part'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Not Found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /simulations/{id}/children:
    get:
      description: Gets a list of all child simulations for a parent simulation
      operationId: getSimulationChildren
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: offset
          in: query
          description: starting paging count; ex. offset of 60 will skip the first 60 items in the list
          required: false
          type: integer
          format: int32
          default: 0
        - name: limit
          in: query
          description: number of materials to return within the query
          required: false
          type: integer
          format: int32
          default: 10
        - name: status
          in: query
          description: simulation status for items retrieved.  If an array of items is sent, they are treated as "OR" operations.
            e.g. status=InProgress,Requested would yield a list of simulations that are in either state.
          required: false
          type: array
          items:
            type: string
            enum:
              - Draft
              - Requested
              - InProgress
              - Cancelled
              - Error
              - Success
        - name: sort
          in: query
          description: key:direction pairs for one or multiple field sort orders.  e.g. sort=key1:desc,key2:asc
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
      responses:
        "200":
          description: Successfully found the list of items
          headers:
            X-Pagination:
              type: string
              description: Contains paging information in json format - totalCount, totalPages
          schema:
            type: array
            items:
              $ref: '#/definitions/Simulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations

  /simulations/{id}/logs:
    get:
      description: Gets a list of all logs for a simulation
      operationId: getSimulationLogs
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: offset
          in: query
          description: starting paging count; ex. offset of 60 will skip the first 60 items in the list
          required: false
          type: integer
          format: int32
          default: 0
        - name: limit
          in: query
          description: number of items to return within the query
          required: false
          type: integer
          format: int32
          default: 10
        - name: sort
          in: query
          description: key:direction pairs for one or multiple field sort orders.  e.g. sort=key1:desc,key2:asc
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
      responses:
        "200":
          description: Successfully found the list of items
          headers:
            X-Pagination:
              type: string
              description: Contains paging information in json format - totalCount, totalPages
          schema:
            type: array
            items:
              $ref: '#/definitions/SimulationLog'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations
    post:
      description: Creates a new log entry in the simulation
      operationId: postSimulationLog
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: SimulationLog
          in: body
          description: A log for a simulation
          required: true
          schema:
            $ref: '#/definitions/SimulationLog'
      responses:
        "200":
          description: Successfully added the item
          schema:
            $ref: '#/definitions/SimulationLog'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /simulations/{id}/outputs:
    get:
      description: Gets a list of all outputs for a simulation
      operationId: getSimulationOutputs
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: offset
          in: query
          description: starting paging count; ex. offset of 60 will skip the first 60 items in the list
          required: false
          type: integer
          format: int32
          default: 0
        - name: limit
          in: query
          description: number of items to return within the query
          required: false
          type: integer
          format: int32
          default: 10
        - name: sort
          in: query
          description: key:direction pairs for one or multiple field sort orders.  e.g. sort=key1:desc,key2:asc
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
      responses:
        "200":
          description: Successfully found the list of items
          headers:
            X-Pagination:
              type: string
              description: Contains paging information in json format - totalCount, totalPages
          schema:
            type: array
            items:
              $ref: '#/definitions/SimulationOutput'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations
    post:
      description: Creates a new output for the simulation
      operationId: postSimulationOutput
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: SimulationOutput
          in: body
          description: An output for a simulation
          required: true
          schema:
            $ref: '#/definitions/SimulationOutput'
      responses:
        "200":
          description: Successfully added the item
          schema:
            $ref: '#/definitions/SimulationOutput'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /simulations/{id}/activities:
    get:
      description: Gets a list of all activities for a simulation
      operationId: getSimulationActivities
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: offset
          in: query
          description: starting paging count; ex. offset of 60 will skip the first 60 items in the list
          required: false
          type: integer
          format: int32
          default: 0
        - name: limit
          in: query
          description: number of items to return within the query
          required: false
          type: integer
          format: int32
          default: 10
        - name: sort
          in: query
          description: key:direction pairs for one or multiple field sort orders.  e.g. sort=key1:desc,key2:asc
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
      responses:
        "200":
          description: Successfully found the list of items
          headers:
            X-Pagination:
              type: string
              description: Contains paging information in json format - totalCount, totalPages
          schema:
            type: array
            items:
              $ref: '#/definitions/SimulationActivity'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations
    post:
      description: Creates a new activity for the simulation
      operationId: postSimulationActivity
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: SimulationActivity
          in: body
          description: An activity for a simulation.  An activity represents the execution of a worker in the simulation pipeline.
          required: true
          schema:
            $ref: '#/definitions/SimulationActivity'
      responses:
        "200":
          description: Successfully added the item
          schema:
            $ref: '#/definitions/SimulationActivity'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /simulations/{id}/activities/{activityId}:
    get:
      description: Gets an activity for a simulation
      operationId: getSimulationActivity
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: activityId
          in: path
          description: activity identifier
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully found the item
          schema:
            $ref: '#/definitions/SimulationActivity'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations
    put:
      description: Updates an activity for a simulation
      operationId: putSimulationActivity
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: activityId
          in: path
          description: activity identifier
          required: true
          type: integer
          format: int32
        - name: SimulationActivity
          in: body
          description: An activity for a simulation.  An activity represents the execution of a worker in the simulation pipeline.
          required: true
          schema:
            $ref: '#/definitions/SimulationActivity'
      responses:
        "200":
          description: Successfully updated the item
          schema:
            $ref: '#/definitions/SimulationActivity'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations
    patch:
      description: Update fields within an activity
      operationId: patchActivity
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: activityId
          in: path
          description: activity identifier
          required: true
          type: integer
          format: int32
        - name: activityPatch
          in: body
          description: This endpoint uses JSON Patch, RFC 6092.
          required: true
          schema:
              type: array
              items:
                $ref: '#/definitions/PatchDocument'
      responses:
        "200":
          description: Successfully patched activity
          schema:
            $ref: '#/definitions/SimulationActivity'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Simulation activity not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /porositysimulations:
    get:
      description: Gets a list of all porosity simulation
      operationId: getPorositySimulations
      parameters:
        - name: organizationId
          in: query
          description: the organization id to get items for.  Must be provided as API callers only have access to items
            belonging to their organization.
          required: true
          type: integer
          format: int32
        - name: status
          in: query
          description: simulation status for items retrieved.  If an array of items is sent, they are treated as "OR" operations.
            e.g. status=InProgress,Requested would yield a list of simulations that are in either state.
          required: false
          type: array
          items:
            type: string
            enum:
              - Draft
              - Requested
              - InProgress
              - Cancelled
              - Error
              - Success
        - name: offset
          in: query
          description: starting paging count; ex. offset of 60 will skip the first 60 items in the list
          required: false
          type: integer
          format: int32
          default: 0
        - name: limit
          in: query
          description: number of materials to return within the query
          required: false
          type: integer
          format: int32
          default: 10
        - name: sort
          in: query
          description: key:direction pairs for one or multiple field sort orders.  e.g. sort=key1:desc,key2:asc
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
      responses:
        "200":
          description: Successfully found the list of items
          headers:
            X-Pagination:
              type: string
              description: Contains paging information in json format - totalCount, totalPages
          schema:
            type: array
            items:
              $ref: '#/definitions/PorositySimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations
    post:
      description: Posts a new porosity simulation
      operationId: postPorositySimulation
      parameters:
        - name: PorositySimulation
          in: body
          description: PorositySimulation fields required to add the simulation
          required: true
          schema:
            $ref: '#/definitions/PorositySimulation'
      responses:
        "200":
          description: Successfully added a simulation
          schema:
            $ref: '#/definitions/PorositySimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /porositysimulations/{id}:
    get:
      description: Get a porosity simulation
      operationId: getPorositySimulation
      parameters:
      - name: id
        in: path
        description: simulation identifier
        required: true
        type: integer
        format: int32
      responses:
        "200":
          description: Successfully retrieved simulation
          schema:
            $ref: '#/definitions/PorositySimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Simulation not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations
    put:
      description: Puts an updated porosity simulation (Only accepts updates to title, description, and tags once simulation is started)
      operationId: putPorositySimulation
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: PorositySimulation
          in: body
          description: PorositySimulation fields required to update a simulation
          required: true
          schema:
            $ref: '#/definitions/PorositySimulation'
      responses:
        "200":
          description: Successfully updated a simulation
          schema:
            $ref: '#/definitions/PorositySimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations
    delete:
      description: This will delete a porosity simulation and all its associated data including output, logs, and any results from completed simulations
      operationId: DeletePorositySimulation
      parameters:
        - name: id
          in: path
          description: ID of simulation
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully deleted a simulation
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Not Found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /porositysimulations/{id}/children:
    get:
      description: Gets a list of all child simulations for a parent simulation
      operationId: getPorositySimulationChildren
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: offset
          in: query
          description: starting paging count; ex. offset of 60 will skip the first 60 items in the list
          required: false
          type: integer
          format: int32
          default: 0
        - name: limit
          in: query
          description: number of materials to return within the query
          required: false
          type: integer
          format: int32
          default: 10
        - name: status
          in: query
          description: simulation status for items retrieved.  If an array of items is sent, they are treated as "OR" operations.
            e.g. status=InProgress,Requested would yield a list of simulations that are in either state.
          required: false
          type: array
          items:
            type: string
            enum:
              - Draft
              - Requested
              - InProgress
              - Cancelled
              - Error
              - Success
        - name: sort
          in: query
          description: key:direction pairs for one or multiple field sort orders.  e.g. sort=key1:desc,key2:asc
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
      responses:
        "200":
          description: Successfully found the list of items
          headers:
            X-Pagination:
              type: string
              description: Contains paging information in json format - totalCount, totalPages
          schema:
            type: array
            items:
              $ref: '#/definitions/PorositySimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations

  /porositysimulations/{id}/cancel:
    put:
      description: Cancels a porosity simulation
      operationId: cancelPorositySimulation
      parameters:
      - name: id
        in: path
        description: simulation identifier
        required: true
        type: integer
        format: int32
      responses:
        "200":
          description: Simulation was successfully cancelled.
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Simulation not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /porositysimulations/{id}/start:
    put:
      description: Starts a porosity simulation.  If the simulation has been started before, start requests have no effect.
      operationId: startPorositySimulation
      parameters:
      - name: id
        in: path
        description: simulation identifier
        required: true
        type: integer
        format: int32
      responses:
        "200":
          description: Simulation was successfully started.
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Simulation not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /singlebeadsimulations:
    get:
      description: Gets a list of all single bead simulations for the active user
      operationId: getSingleBeadSimulations
      parameters:
        - name: organizationId
          in: query
          description: the organization id to get items for.  Must be provided as API callers only have access to items
            belonging to their organization.
          required: true
          type: integer
          format: int32
        - name: status
          in: query
          description: simulation status for items retrieved.  If an array of items is sent, they are treated as "OR" operations.
            e.g. status=InProgress,Requested would yield a list of simulations that are in either state.
          required: false
          type: array
          items:
            type: string
            enum:
              - Draft
              - Requested
              - InProgress
              - Cancelled
              - Error
              - Success
        - name: offset
          in: query
          description: starting paging count; ex. offset of 60 will skip the first 60 items in the list
          required: false
          type: integer
          format: int32
          default: 0
        - name: limit
          in: query
          description: number of items to return within the query
          required: false
          type: integer
          format: int32
          default: 10
        - name: sort
          in: query
          description: key:direction pairs for one or multiple field sort orders.  e.g. sort=key1:desc,key2:asc
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
      responses:
        "200":
          description: Successfully found the list of items
          headers:
            X-Pagination:
              type: string
              description: Contains paging information in json format - totalCount, totalPages
          schema:
            type: array
            items:
              $ref: '#/definitions/SingleBeadSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations
    post:
      description: Posts a new single bead simulation
      operationId: postSingleBeadSimulation
      parameters:
        - name: SingleBeadSimulation
          in: body
          description: SingleBeadSimulation fields required to add the simulation
          required: true
          schema:
            $ref: '#/definitions/SingleBeadSimulation'
      responses:
        "200":
          description: Successfully added a simulation
          schema:
            $ref: '#/definitions/SingleBeadSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /singlebeadsimulations/{id}:
    get:
      description: Get a single bead simulation
      operationId: getSingleBeadSimulation
      parameters:
      - name: id
        in: path
        description: simulation identifier
        required: true
        type: integer
        format: int32
      responses:
        "200":
          description: Successfully retrieved simulation
          schema:
            $ref: '#/definitions/SingleBeadSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Simulation not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations
    put:
      description: Puts an updated single bead simulation (Only accepts updates to title, description, and tags once simulation is started)
      operationId: putSingleBeadSimulation
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: SingleBeadSimulation
          in: body
          description: SingleBeadSimulation fields required to update a simulation
          required: true
          schema:
            $ref: '#/definitions/SingleBeadSimulation'
      responses:
        "200":
          description: Successfully updated a simulation
          schema:
            $ref: '#/definitions/SingleBeadSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations
    delete:
      description: This will delete a single bead simulation and all its associated data including output, logs, and any results from completed simulations
      operationId: DeleteSingleBeadSimulation
      parameters:
        - name: id
          in: path
          description: ID of simulation
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully deleted a simulation
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Not Found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /singlebeadsimulations/{id}/children:
    get:
      description: Gets a list of all child simulations for a parent simulation
      operationId: getSingleBeadSimulationChildren
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: offset
          in: query
          description: starting paging count; ex. offset of 60 will skip the first 60 items in the list
          required: false
          type: integer
          format: int32
          default: 0
        - name: limit
          in: query
          description: number of materials to return within the query
          required: false
          type: integer
          format: int32
          default: 10
        - name: status
          in: query
          description: simulation status for items retrieved.  If an array of items is sent, they are treated as "OR" operations.
            e.g. status=InProgress,Requested would yield a list of simulations that are in either state.
          required: false
          type: array
          items:
            type: string
            enum:
              - Draft
              - Requested
              - InProgress
              - Cancelled
              - Error
              - Success
        - name: sort
          in: query
          description: key:direction pairs for one or multiple field sort orders.  e.g. sort=key1:desc,key2:asc
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
      responses:
        "200":
          description: Successfully found the list of items
          headers:
            X-Pagination:
              type: string
              description: Contains paging information in json format - totalCount, totalPages
          schema:
            type: array
            items:
              $ref: '#/definitions/SingleBeadSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations

  /singlebeadsimulations/{id}/cancel:
    put:
      description: Cancels a single bead simulation
      operationId: cancelSingleBeadSimulation
      parameters:
      - name: id
        in: path
        description: simulation identifier
        required: true
        type: integer
        format: int32
      responses:
        "200":
          description: Simulation was successfully cancelled.
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Simulation not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /singlebeadsimulations/{id}/start:
    put:
      description: Starts a single bead simulation.  If the simulation has been started before, start requests have no effect.
      operationId: startSingleBeadSimulation
      parameters:
      - name: id
        in: path
        description: simulation identifier
        required: true
        type: integer
        format: int32
      responses:
        "200":
          description: Simulation was successfully started.
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Simulation not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /thermalsimulations:
    get:
      description: Gets a list of all thermal simulations for the active user
      operationId: getThermalSimulations
      parameters:
        - name: organizationId
          in: query
          description: the organization id to get items for.  Must be provided as API callers only have access to items
            belonging to their organization.
          required: true
          type: integer
          format: int32
        - name: status
          in: query
          description: simulation status for items retrieved.  If an array of items is sent, they are treated as "OR" operations.
            e.g. status=InProgress,Requested would yield a list of simulations that are in either state.
          required: false
          type: array
          items:
            type: string
            enum:
              - Draft
              - Requested
              - InProgress
              - Cancelled
              - Error
              - Success
        - name: offset
          in: query
          description: starting paging count; ex. offset of 60 will skip the first 60 items in the list
          required: false
          type: integer
          format: int32
          default: 0
        - name: limit
          in: query
          description: number of items to return within the query
          required: false
          type: integer
          format: int32
          default: 10
        - name: sort
          in: query
          description: key:direction pairs for one or multiple field sort orders.  e.g. sort=key1:desc,key2:asc
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
      responses:
        "200":
          description: Successfully found the list of items
          headers:
            X-Pagination:
              type: string
              description: Contains paging information in json format - totalCount, totalPages
          schema:
            type: array
            items:
              $ref: '#/definitions/ThermalSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations
    post:
      description: Posts a new thermal simulation
      operationId: postThermalSimulation
      parameters:
        - name: ThermalSimulation
          in: body
          description: ThermalSimulation fields required to add a simulation
          required: true
          schema:
            $ref: '#/definitions/ThermalSimulation'
      responses:
        "200":
          description: Successfully added a simulation
          schema:
            $ref: '#/definitions/ThermalSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /thermalsimulations/{id}:
    get:
      description: Get a thermal simulation
      operationId: getThermalSimulation
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully retrieved simulation
          schema:
            $ref: '#/definitions/ThermalSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Simulation not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations
    put:
      description: Puts an updated thermal simulation (Only accepts updates to title, description, and tags once simulation is started)
      operationId: putThermalSimulation
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: ThermalSimulation
          in: body
          description: ThermalSimulation fields required to update a simulation
          required: true
          schema:
            $ref: '#/definitions/ThermalSimulation'
      responses:
        "200":
          description: Successfully updated a simulation
          schema:
            $ref: '#/definitions/ThermalSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations
    delete:
      description: This will delete a thermal simulation and all its associated data including output, logs, and any results from completed simulations
      operationId: DeleteThermalSimulation
      parameters:
        - name: id
          in: path
          description: ID of simulation
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully deleted a simulation
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Not Found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /thermalsimulations/{id}/children:
    get:
      description: Gets a list of all child simulations for a parent simulation
      operationId: getThermalSimulationChildren
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: offset
          in: query
          description: starting paging count; ex. offset of 60 will skip the first 60 items in the list
          required: false
          type: integer
          format: int32
          default: 0
        - name: limit
          in: query
          description: number of materials to return within the query
          required: false
          type: integer
          format: int32
          default: 10
        - name: status
          in: query
          description: simulation status for items retrieved.  If an array of items is sent, they are treated as "OR" operations.
            e.g. status=InProgress,Requested would yield a list of simulations that are in either state.
          required: false
          type: array
          items:
            type: string
            enum:
              - Draft
              - Requested
              - InProgress
              - Cancelled
              - Error
              - Success
        - name: sort
          in: query
          description: key:direction pairs for one or multiple field sort orders.  e.g. sort=key1:desc,key2:asc
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
      responses:
        "200":
          description: Successfully found the list of items
          headers:
            X-Pagination:
              type: string
              description: Contains paging information in json format - totalCount, totalPages
          schema:
            type: array
            items:
              $ref: '#/definitions/ThermalSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations

  /thermalsimulations/{id}/cancel:
    put:
      description: Cancels a thermal simulation
      operationId: cancelThermalSimulation
      parameters:
      - name: id
        in: path
        description: simulation identifier
        required: true
        type: integer
        format: int32
      responses:
        "200":
          description: Simulation was successfully cancelled.
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Simulation not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /thermalsimulations/{id}/start:
    put:
      description: Starts a thermal simulation.  If the simulation has been started before, start requests have no effect.
      operationId: startThermalSimulation
      parameters:
      - name: id
        in: path
        description: simulation identifier
        required: true
        type: integer
        format: int32
      responses:
        "200":
          description: Simulation was successfully started.
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Simulation not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /scanpatternsimulations:
    get:
      description: Gets a list of all scan pattern simulations for the active user
      operationId: getScanPatternSimulations
      parameters:
        - name: organizationId
          in: query
          description: the organization id to get items for.  Must be provided as API callers only have access to items
            belonging to their organization.
          required: true
          type: integer
          format: int32
        - name: status
          in: query
          description: simulation status for items retrieved.  If an array of items is sent, they are treated as "OR" operations.
            e.g. status=InProgress,Requested would yield a list of simulations that are in either state.
          required: false
          type: array
          items:
            type: string
            enum:
              - Draft
              - Requested
              - InProgress
              - Cancelled
              - Error
              - Success
        - name: offset
          in: query
          description: starting paging count; ex. offset of 60 will skip the first 60 items in the list
          required: false
          type: integer
          format: int32
          default: 0
        - name: limit
          in: query
          description: number of items to return within the query
          required: false
          type: integer
          format: int32
          default: 10
        - name: sort
          in: query
          description: key:direction pairs for one or multiple field sort orders.  e.g. sort=key1:desc,key2:asc
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
      responses:
        "200":
          description: Successfully found the list of items
          headers:
            X-Pagination:
              type: string
              description: Contains paging information in json format - totalCount, totalPages
          schema:
            type: array
            items:
              $ref: '#/definitions/ScanPatternSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations
    post:
      description: Posts a new scan pattern simulation
      operationId: postScanPatternSimulation
      parameters:
        - name: ScanPatternSimulation
          in: body
          description: ScanPatternSimulation fields required to add a simulation
          required: true
          schema:
            $ref: '#/definitions/ScanPatternSimulation'
      responses:
        "200":
          description: Successfully added a simulation
          schema:
            $ref: '#/definitions/ScanPatternSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /scanpatternsimulations/{id}:
    get:
      description: Get a Scan Pattern simulation
      operationId: getScanPatternSimulation
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully retrieved simulation
          schema:
            $ref: '#/definitions/ScanPatternSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Simulation not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations
    put:
      description: Puts an updated scan pattern simulation (Only accepts updates to title, description, and tags once simulation is started)
      operationId: putScanPatternSimulation
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: ScanPatternSimulation
          in: body
          description: ScanPatternSimulation fields required to update a simulation
          required: true
          schema:
            $ref: '#/definitions/ScanPatternSimulation'
      responses:
        "200":
          description: Successfully updated a simulation
          schema:
            $ref: '#/definitions/ScanPatternSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations
    delete:
      description: This will delete a scan pattern simulation and all its associated data including output, logs, and any results from completed simulations
      operationId: DeleteScanPatternSimulation
      parameters:
        - name: id
          in: path
          description: ID of simulation
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully deleted a simulation
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Not Found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /scanpatternsimulations/{id}/children:
    get:
      description: Gets a list of all child simulations for a parent simulation
      operationId: getScanPatternSimulationChildren
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: offset
          in: query
          description: starting paging count; ex. offset of 60 will skip the first 60 items in the list
          required: false
          type: integer
          format: int32
          default: 0
        - name: limit
          in: query
          description: number of materials to return within the query
          required: false
          type: integer
          format: int32
          default: 10
        - name: status
          in: query
          description: simulation status for items retrieved.  If an array of items is sent, they are treated as "OR" operations.
            e.g. status=InProgress,Requested would yield a list of simulations that are in either state.
          required: false
          type: array
          items:
            type: string
            enum:
              - Draft
              - Requested
              - InProgress
              - Cancelled
              - Error
              - Success
        - name: sort
          in: query
          description: key:direction pairs for one or multiple field sort orders.  e.g. sort=key1:desc,key2:asc
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
      responses:
        "200":
          description: Successfully found the list of items
          headers:
            X-Pagination:
              type: string
              description: Contains paging information in json format - totalCount, totalPages
          schema:
            type: array
            items:
              $ref: '#/definitions/ScanPatternSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations

  /scanpatternsimulations/{id}/cancel:
    put:
      description: Cancels a Scan Pattern simulation
      operationId: cancelScanPatternSimulation
      parameters:
      - name: id
        in: path
        description: simulation identifier
        required: true
        type: integer
        format: int32
      responses:
        "200":
          description: Simulation was successfully cancelled.
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Simulation not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /scanpatternsimulations/{id}/start:
    put:
      description: Starts a Scan Pattern simulation.  If the simulation has been started before, start requests have no effect.
      operationId: startScanPatternSimulation
      parameters:
      - name: id
        in: path
        description: simulation identifier
        required: true
        type: integer
        format: int32
      responses:
        "200":
          description: Simulation was successfully started.
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Simulation not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /assumedstrainsimulations:
    get:
      description: Gets a list of all assumed strain simulations for the active user
      operationId: getAssumedStrainSimulations
      parameters:
        - name: organizationId
          in: query
          description: the organization id to get items for.  Must be provided as API callers only have access to items
            belonging to their organization.
          required: true
          type: integer
          format: int32
        - name: status
          in: query
          description: simulation status for items retrieved.  If an array of items is sent, they are treated as "OR" operations.
            e.g. status=InProgress,Requested would yield a list of simulations that are in either state.
          required: false
          type: array
          items:
            type: string
            enum:
              - Draft
              - Requested
              - InProgress
              - Cancelled
              - Error
              - Success
        - name: offset
          in: query
          description: starting paging count; ex. offset of 60 will skip the first 60 items in the list
          required: false
          type: integer
          format: int32
          default: 0
        - name: limit
          in: query
          description: number of items to return within the query
          required: false
          type: integer
          format: int32
          default: 10
        - name: sort
          in: query
          description: key:direction pairs for one or multiple field sort orders.  e.g. sort=key1:desc,key2:asc
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
      responses:
        "200":
          description: Successfully found the list of items
          headers:
            X-Pagination:
              type: string
              description: Contains paging information in json format - totalCount, totalPages
          schema:
            type: array
            items:
              $ref: '#/definitions/AssumedStrainSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations
    post:
      description: Posts a new assumed strain simulation
      operationId: postAssumedStrainSimulation
      parameters:
        - name: AssumedStrainSimulation
          in: body
          description: AssumedStrainSimulation fields required to add a simulation
          required: true
          schema:
            $ref: '#/definitions/AssumedStrainSimulation'
      responses:
        "200":
          description: Successfully added a simulation
          schema:
            $ref: '#/definitions/AssumedStrainSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /assumedstrainsimulations/{id}:
    get:
      description: Get a assumed strain simulation
      operationId: getAssumedStrainSimulation
      parameters:
      - name: id
        in: path
        description: simulation identifier
        required: true
        type: integer
        format: int32
      responses:
        "200":
          description: Successfully retrieved simulation
          schema:
            $ref: '#/definitions/AssumedStrainSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Simulation not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations
    put:
      description: Puts an updated assumed strain simulation (Only accepts updates to title, description, and tags once simulation is started)
      operationId: putAssumedStrainSimulation
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: AssumedStrainSimulation
          in: body
          description: AssumedStrainSimulation fields required to update a simulation
          required: true
          schema:
            $ref: '#/definitions/AssumedStrainSimulation'
      responses:
        "200":
          description: Successfully updated a simulation
          schema:
            $ref: '#/definitions/AssumedStrainSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations
    delete:
      description: This will delete an assumed strain simulation and all its associated data including output, logs, and any results from completed simulations
      operationId: DeleteAssumedStrainSimulation
      parameters:
        - name: id
          in: path
          description: ID of simulation
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully deleted a simulation
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Not Found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /assumedstrainsimulations/{id}/children:
    get:
      description: Gets a list of all child simulations for a parent simulation
      operationId: getAssumedStrainSimulationChildren
      parameters:
        - name: id
          in: path
          description: simulation identifier
          required: true
          type: integer
          format: int32
        - name: offset
          in: query
          description: starting paging count; ex. offset of 60 will skip the first 60 items in the list
          required: false
          type: integer
          format: int32
          default: 0
        - name: limit
          in: query
          description: number of materials to return within the query
          required: false
          type: integer
          format: int32
          default: 10
        - name: status
          in: query
          description: simulation status for items retrieved.  If an array of items is sent, they are treated as "OR" operations.
            e.g. status=InProgress,Requested would yield a list of simulations that are in either state.
          required: false
          type: array
          items:
            type: string
            enum:
              - Draft
              - Requested
              - InProgress
              - Cancelled
              - Error
              - Success
        - name: sort
          in: query
          description: key:direction pairs for one or multiple field sort orders.  e.g. sort=key1:desc,key2:asc
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
      responses:
        "200":
          description: Successfully found the list of items
          headers:
            X-Pagination:
              type: string
              description: Contains paging information in json format - totalCount, totalPages
          schema:
            type: array
            items:
              $ref: '#/definitions/AssumedStrainSimulation'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations

  /assumedstrainsimulations/{id}/cancel:
    put:
      description: Cancels a assumed strain simulation
      operationId: cancelAssumedStrainSimulation
      parameters:
      - name: id
        in: path
        description: simulation identifier
        required: true
        type: integer
        format: int32
      responses:
        "200":
          description: Simulation was successfully cancelled.
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Simulation not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /assumedstrainsimulations/{id}/start:
    put:
      description: Starts an assumed strain simulation.  If the simulation has been started before, start requests have no effect.
      operationId: startAssumedStrainSimulation
      parameters:
      - name: id
        in: path
        description: simulation identifier
        required: true
        type: integer
        format: int32
      responses:
        "200":
          description: Simulation was successfully started.
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Simulation not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:simulations

  /assumedstrainsimulations/{id}/estimate:
    get:
      description: Get an estimate, in minutes, for running an assumed strain simulation for the selected part and specified voxel size.
      operationId: AssumedStrainEstimate
      parameters:
        - name: id
          in: path
          description: ID of simulation
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully estimated time for assumed strain simulation
          schema:
            $ref: '#/definitions/SimulationEstimate'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Not Found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:simulations

  /buildfiles:
    get:
      description: Get list of build files
      operationId: getBuildFiles
      parameters:
        - name: organizationId
          in: query
          description: the organization id to get items for.  Must be provided as API callers only have access to items
            belonging to their organization.
          required: true
          type: integer
          format: int32
        - name: offset
          in: query
          description: starting paging count; ex. 60 will skip the first 60 items in the list
          required: false
          type: integer
          format: int32
          default: 0
        - name: limit
          in: query
          description: number of items to return within the query
          required: false
          type: integer
          format: int32
          default: 10
        - name: sort
          in: query
          description: key:direction pairs for one or multiple field sort orders.  e.g. sort=key1:desc,key2:asc
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
        - name: archived
          in: query
          description: If true, will only return archived items.  If false, will only return unarchived items.  If not
            provided, will return both archived and unarchived items.
          required: false
          type: boolean
        - name: availability
          in: query
          description: 'availability value of build files to return, valid values: Uploaded, Processing, Available, Error'
          required: false
          type: array
          items:
            type: string
            enum:
              - Uploaded
              - Processing
              - Available
              - Error
      responses:
        "200":
          description: Successfully retrieved list of build files
          headers:
            X-Pagination:
              type: string
              description: Contains paging information in json format - totalCount, totalPages
          schema:
            type: array
            items:
              $ref: '#/definitions/BuildFile'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:buildFiles
    post:
      description: Add a new build file
      operationId: postBuildFile
      parameters:
        - name: buildFilePost
          in: body
          description: build file to add
          required: true
          schema:
            $ref: '#/definitions/BuildFilePost'
      responses:
        "200":
          description: Build file that was successfully added
          schema:
            $ref: '#/definitions/BuildFile'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:buildFiles

  /buildfiles/buildfileuploadurl:
    get:
      description: Returns a url used to upload a ZIP file containing build file components. The returned url is used in a post to buildfiles/.
      operationId: getBuildFileUploadUrl
      responses:
        "200":
          description: URL to use for uploading a ZIP file containing build file components
          schema:
            type: string
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'

  /buildfiles/{id}:
    get:
      description: Retrieve a single build file
      operationId: getBuildFile
      parameters:
        - name: id
          in: path
          description: ID of build file to retrieve
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Retrieved build file
          schema:
            $ref: '#/definitions/BuildFile'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: build file not found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:buildFiles
    put:
      description: Update a single build file
      operationId: updateBuildFile
      parameters:
        - name: id
          in: path
          description: ID of build file to update
          required: true
          type: integer
          format: int32
        - name: buildFile
          in: body
          description: build file to update
          required: true
          schema:
            $ref: '#/definitions/BuildFile'
      responses:
        "200":
          description: Updated build file
          schema:
            $ref: '#/definitions/BuildFile'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: build file not found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:buildFiles
    patch:
      description: Update fields within a build file
      operationId: patchBuildFile
      parameters:
        - name: id
          in: path
          description: ID of build file to update
          required: true
          type: integer
          format: int32
        - name: buildFilePatch
          in: body
          description: This endpoint uses JSON Patch, RFC 6092.
          required: true
          schema:
              type: array
              items:
                $ref: '#/definitions/PatchDocument'
      responses:
        "200":
          description: Patched build file
          schema:
            $ref: '#/definitions/BuildFile'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Build file not found (id invalid)
          schema:
            $ref: '#/definitions/Error'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:buildFiles
    delete:
      description: This will delete a build file
      operationId: DeleteBuildFile
      parameters:
        - name: id
          in: path
          description: ID of build file
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Successfully deleted the build file
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: Not Found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - write:buildFiles

  /buildfiles/{id}/geometry:
    get:
      description: Retrieve the part geometry associated with a specific build file as a list of triangles
      operationId: getBuildFileGeometry
      parameters:
        - name: id
          in: path
          description: ID of build file to retrieve geometry of
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: Part geometry as an array of triangles
          schema:
            type: array
            items:
              $ref: '#/definitions/Triangle'
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: build file not found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:buildFiles

  /buildfiles/{id}/geometryurl:
    get:
      description: Get a url to the part geometry STL file associated with a specific build file
      operationId: getBuildFileGeometryUrl
      parameters:
        - name: id
          in: path
          description: ID of build file to retrieve geometry of
          required: true
          type: integer
          format: int32
      responses:
        "200":
          description: URL to the part STL file
          schema:
            type: string
        "401":
          description: Not authorized
          schema:
            $ref: '#/definitions/Error'
        "403":
          description: Forbidden
          schema:
            $ref: '#/definitions/Error'
        "404":
          description: build file not found
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
      security:
        - simulationSecurity:
          - read:buildFiles

securityDefinitions:
  simulationSecurity:
    type: oauth2
    authorizationUrl: https://3dsim.auth0.com/authorize
    flow: implicit
    scopes:
      write:simulations: create and update simulations
      read:simulations: read simulations
      write:parts: create and update parts
      read:parts: read parts
      write:machines: create and update machines
      read:machines: read machines
      write:materials: create and update materials
      read:materials: read materials
      write:buildFiles: write build files
      read:buildFiles: read build files

definitions:
  SimulationActivity:
    type: object
    required:
      - simulationId
      - activityId
      - activityName
      - workerVersion
    properties:
      id:
        type: integer
        format: int32
      simulationId:
        type: integer
        format: int32
        description: id of associated simulation, set server-side, read-only
      activityId:
        type: string
        description: the Amazon SWF activity id
      activityName:
        type: string
        description: the name of the Amazon SWF activity
      workerVersion:
        type: string
        description: the version of the worker, e.g. 1.9.0
      startedAt:
        type: string
        format: date-time
        description: started time stamp
      completedAt:
        type: string
        format: date-time
        description: completed time stamp
        x-nullable: true
      percentComplete:
        type: integer
        format: int32
        description: number 0 - 100 representing percentage of activity already completed
      status:
        type: string
        description: status of the activity
        enum:
          - Waiting
          - Running
          - Cancelled
          - Error
          - Finished

  SimulationOutput:
    type: object
    required:
     - label
     - fileLocation
     - createdAt
    properties:
      id:
        type: integer
        format: int32
        description: id of associated simulation, set server-side, read-only
      simulationId:
        type: integer
        format: int32
      createdAt:
        type: string
        format: date-time
        description: created time stamp
      label:
        type: string
        description: User readable label of what the output contains
      fileLocation:
        type: string
        description: Location of file, usually an S3 location

  SimulationPart:
    type: object
    description: "As-run part related parameters used for a simulation. If the part field is included in a Simulation PUT or POST, it will not be updated."
    required:
      - partId
      - simulationId
      - xMin
      - yMin
      - zMin
    properties:
      id:
        type: integer
        format: int32
      partId:
        description: "ID of the associated part"
        type: integer
        format: int32
      part:
        $ref: "#/definitions/Part"
      simulationId:
        description: "ID of the associated simulation"
        type: integer
        format: int32
      xMin:
        description: "Smallest x coordinate of the part location on the powder bed in millimeters. The center of the powder bed in the x dimension is assumed to be 0"
        type: number
        minimum: -125
        maximum: 125
        exclusiveMinimum: false
        exclusiveMaximum: false
      yMin:
        description: "Smallest y coordinate of the part location on the powder bed in millimeters. The center of the powder bed in the y dimension is assumed to be 0"
        type: number
        minimum: -125
        maximum: 125
        exclusiveMinimum: false
        exclusiveMaximum: false
      zMin:
        description: "Not used at present. This must be set to 0"
        type: number
        minimum: 0
        maximum: 0
        exclusiveMinimum: false
        exclusiveMaximum: false
      partSupportId:
        type: integer
        format: int32
        description: Id of the support to use for the simulation. Can be null
      support:
        $ref: "#/definitions/PartSupport"


  SimulationLog:
    type: object
    required:
      - message
      - loggedAt
    properties:
      id:
        type: integer
        format: int32
      simulationId:
        type: integer
        format: int32
        description: id of associated simulation, set server-side, read-only
      loggedAt:
        type: string
        format: date-time
        description: time stamp of log
      message:
        type: string
      level:
        type: string
        description: level of the log
        enum:
          - Info
          - Trace
          - Error
          - Warn
      activityId:
        type: string
        description: the Amazon SWF activity id

  Status:
    required:
      - status
    properties:
      status:
        type: string
        enum:
          - okay
          - problem
          - slow
      message:
        type: string

  SimulationType:
    type: string
    enum:
      - SingleBeadSimulation
      - ThermalSimulation
      - PorositySimulation
      - AssumedStrainSimulation
      - ScanPatternSimulation

  Simulation:
    type: object
    required:
      - title
      - organizationId
    properties:
      id:
        type: integer
        format: int32
      type:
        $ref: "#/definitions/SimulationType"
      organizationId:
        type: integer
        format: int32
      parentId:
        type: integer
        format: int32
        x-nullable: true
      title:
        type: string
      description:
        type: string
      tags:
        type: array
        items:
          type: string
      status:
        type: string
        enum:
          - Draft
          - Requested
          - InProgress
          - Cancelled
          - Error
          - Success
      archived:
        type: boolean
        description: True if simulation has been archived, false otherwise
      materialId:
        type: integer
        format: int32
        description: ID of the material to use in the simulation
      materialConfigurationId:
        type: integer
        format: int32
        description: ID of the material configuration that was used in the simulation, set server-side, read only
      machineId:
        type: integer
        format: int32
        description: ID of the machine to use in the simulation
      machineConfigurationId:
        type: integer
        format: int32
        description: ID of the machine configuration that was used in the simulation, set server-side, read only
      started:
        type: string
        format: date-time
        description: time stamp, set server-side, read only
      startedBy:
        type: string
        description: set server-side, read only
      completed:
        type: string
        format: date-time
        description: time stamp, set server-side, read only
      percentComplete:
        type: number
        description: set server-side, read only
      created:
        type: string
        format: date-time
        description: created time stamp, set server-side, read only
      createdBy:
        type: string
        description: assigned user, set server-side, read only
      lastModified:
        type: string
        format: date-time
        description: last modified time stamp, set server-side, read only
      lastModifiedBy:
        type: string
        description: assigned user, set server-side, read only
      simulationFolder:
        type: string
        description: s3 folder used to store input and output files for the simulation
      workflowId:
        type: string
        description: workflow identifier - will be provided by the system
      numberOfCores:
        type: integer
        format: int32
        description: indicate the number of cores with which to run the simulation
      voxelLayers:
        type: integer
        format: int32
        description: number of voxel layers in the voxelized part + supports
      sliceLayers:
        type: integer
        format: int32
        description: number of slice layers in the part

  PorositySimulationParameters:
    required:
      - geometryWidth
      - geometryLength
      - geometryHeight
      - startingLayerAngle
      - layerRotationAngle
      - layerThicknessValues
      - laserWattageValues
      - scanSpeedValues
      - hatchSpacingValues
      - slicingStripeWidthValues
      - originXValues
      - originYValues
      - solidusTemperatureValues
    properties:
      geometryWidth:
        description: Must be between 0.001 to 0.01 meters
        type: number
        minimum: 0.001
        maximum: 0.01
        exclusiveMinimum: false
        exclusiveMaximum: false
      geometryLength:
        description: Must be between 0.001 to 0.01 meters
        type: number
        minimum: 0.001
        maximum: 0.01
        exclusiveMinimum: false
        exclusiveMaximum: false
      geometryHeight:
        description: Must be between 0.001 to 0.01 meters
        type: number
        minimum: 0.001
        maximum: 0.01
        exclusiveMinimum: false
        exclusiveMaximum: false
      startingLayerAngle:
        description: Must be between 0 to 180 degrees
        type: number
        minimum: 0
        maximum: 180
        exclusiveMinimum: false
        exclusiveMaximum: false
      layerRotationAngle:
        description: Must be between 0 to 180 degrees
        type: number
        minimum: 0
        maximum: 180
        exclusiveMinimum: false
        exclusiveMaximum: false
      layerThicknessValues:
        description: Array of layer thickness values to simulate across, Each value must be between 0.00001 to 0.0001 meters
        type: array
        items:
          type: number
          minimum: 0.00001
          maximum: 0.0001
          exclusiveMinimum: false
          exclusiveMaximum: false
      laserWattageValues:
        description: Array of laser power values to simulate across, Each value must be between 10 to 1000 watts
        type: array
        items:
          type: number
          minimum: 10
          maximum: 1000
          exclusiveMinimum: false
          exclusiveMaximum: false
      scanSpeedValues:
        description: Array of scan speed values to simulate across, Each value must be between 0.01 to 10 meters/second
        type: array
        items:
          type: number
          minimum: 0.01
          maximum: 10
          exclusiveMinimum: false
          exclusiveMaximum: false
      hatchSpacingValues:
        description: Array of hatch spacing values to simulate across, Each value must be between 0.00001 to 0.001 meters
        type: array
        items:
          type: number
          minimum: 0.00001
          maximum: 0.001
          exclusiveMinimum: false
          exclusiveMaximum: false
      slicingStripeWidthValues:
        description: Array of slicing strip width values to simulate across, Each value must be between 0.001 to 0.1 meters
        type: array
        items:
          type: number
          minimum: 0.001
          maximum: 0.1
          exclusiveMinimum: false
          exclusiveMaximum: false
      originXValues:
        description: Array of origin x values to simulate across, Each value must be between 0 to 0.2 meters
        type: array
        items:
          type: number
          minimum: 0
          maximum: 0.2
          exclusiveMinimum: false
          exclusiveMaximum: false
      originYValues:
        description: Array of origin y values to simulate across, Each value must be between 0 to 0.2 meters
        type: array
        items:
          type: number
          minimum: 0
          maximum: 0.2
          exclusiveMinimum: false
          exclusiveMaximum: false
      solidusTemperatureValues:
        description: Array of solidus temperature values to simulate across
        type: array
        items:
          type: number

  PorositySimulation:
    allOf:
      - $ref: "#/definitions/Simulation"
      - $ref: "#/definitions/PorositySimulationParameters"

  SingleBeadSimulationParameters:
    required:
      - beadType
      - beadLength
      - layerThickness
      - laserWattageValues
      - scanSpeedValues
      - powderThermalConductivityValues
      - powderPackingDensityValues
      - laserAbsorptivityInPowderValues
      - laserAbsorptivityInSolidValues
      - solidusTemperatureValues
      - liquidusTemperatureValues
    properties:
      beadType:
        description: Type of single bead simulation - either bead on plate or bead on powder.
        type: string
        enum:
          - OnPlate
          - OnPowder
      beadLength:
        description: Must be between 0.001 to 0.01 meters
        type: number
        minimum: 0.001
        maximum: 0.01
        exclusiveMinimum: false
        exclusiveMaximum: false
      layerThickness:
        description: Must be between 0.00001 to 0.0001 meters
        type: number
        minimum: 0.00001
        maximum: 0.0001
        exclusiveMinimum: false
        exclusiveMaximum: false
      laserWattageValues:
        description: Array of laser power values to simulate across, Each value must be between 10 to 1000 watts
        type: array
        items:
          type: number
          minimum: 10
          maximum: 1000
          exclusiveMinimum: false
          exclusiveMaximum: false
      scanSpeedValues:
        description: Array of scan speed values to simulate across, Each value must be between 0.01 to 10 meters/second
        type: array
        items:
          type: number
          minimum: 0.01
          maximum: 10
          exclusiveMinimum: false
          exclusiveMaximum: false
      powderThermalConductivityValues:
        description: Array of Powder Thermal Conductivity Percentage Values to simulate across, Each value must be between 0% and 100%, expressed as a decimal
        type: array
        items:
          type: number
          minimum: 0
          maximum: 1
          exclusiveMinimum: false
          exclusiveMaximum: false
      powderPackingDensityValues:
        description: Array of Powder Packing Density Percentage Values to simulate across, Each value must be between 0% and 100%, expressed as a decimal
        type: array
        items:
          type: number
          minimum: 0
          maximum: 1
          exclusiveMinimum: false
          exclusiveMaximum: false
      laserAbsorptivityInPowderValues:
        description: Array of Powder Laser Absorptivity Values to simulate across, Each value must be between 0% and 100%, expressed as a decimal
        type: array
        items:
          type: number
          minimum: 0
          maximum: 1
          exclusiveMinimum: false
          exclusiveMaximum: false
      laserAbsorptivityInSolidValues:
        description: Array of Solid Laser Absorptivity Values to simulate across, Each value must be between 0% and 100%, expressed as a decimal
        type: array
        items:
          type: number
          minimum: 0
          maximum: 1
          exclusiveMinimum: false
          exclusiveMaximum: false
      solidusTemperatureValues:
        description: Array of Solidus Temperature Values Values to simulate across
        type: array
        items:
          type: number
      liquidusTemperatureValues:
        description: Array of Liquidus Temperature Values Values to simulate across
        type: array
        items:
          type: number

  PartBasedSimulationParameters:
    required:
      - supportAngle
      - supportFactorOfSafety
      - minimumWallThickness
      - maximumWallThickness
      - minimumWallDistance
      - maximumWallDistance
      - minimumSupportHeight
      - voxelSize
      - supportYieldStrength
      - supportYieldStrengthRatio
      - elasticModulus
      - poissonRatio
      - strainScalingFactor
      - outputDisplacementAfterCutoff
      - performSupportOptimization
      - generateSupportVoxels
      - stressMode
      - detectBladeCrash
      - outputLayerVtk
      - supportType
    properties:
      simulationParts:
        description: List of parts to simulate (current limit is one part, imposed by server)
        type: array
        items:
          $ref: "#/definitions/SimulationPart"
      supportFileLocation:
        type: string
      partFileLocation:
        type: string
      supportYieldStrength:
        type: number
      supportYieldStrengthRatio:
        type: number
      elasticModulus:
        type: number
      poissonRatio:
        type: number
      strainScalingFactor:
        type: number
      buildFileId:
        description: Id of build file being simulated, mutually exclusive with simulationParts
        type: integer
        format: int32
      performSupportOptimization:
        type: boolean
      voxelSize:
        description: Must be between 0.00002 to 0.002 meters
        type: number
        minimum: 0.00002
        maximum: 0.002
      minimumWallThickness:
        description: Must be between 0.00005 to 0.0003 meters, Must be less than maximumWallThickness
        type: number
        minimum: 0.00005
        maximum: 0.0003
      maximumWallThickness:
       description: Must be between 0.00015 to 0.002 meters, Must be greater than minimumWallThickness
       type: number
       minimum: 0.00015
       maximum: 0.002
      minimumWallDistance:
       description: Must be between 0 to 0.0003 meters, Must be less than maximumWallDistance
       type: number
       minimum: 0
       maximum: 0.0003
      maximumWallDistance:
       description: Must be between 0 to 0.005 meters, Must be greater than minimumWallDistance
       type: number
       minimum: 0
       maximum: 0.005
      minimumSupportHeight:
       description: Distance to move the part off the base plate for supports, Must be between 0 to 0.005 meters
       type: number
       minimum: 0
       maximum: 0.005
      generateSupportVoxels:
        type: boolean
      outputDisplacementAfterCutoff:
        type: boolean
      stressMode:
        type: string
        enum:
          - LinearElastic
          - J2Plasticity
      supportAngle:
        description: Must be between 1 to 89 degrees
        type: number
        minimum: 1
        maximum: 89
      supportFactorOfSafety:
        description: Multiplier for support calculations, Must be between 0.1 to 10
        type: number
        minimum: 0.1
        maximum: 10
      outputLayerVtk:
        type: boolean
        description: if true, mechanics solver output will include a zip file with the stress / distortion state at the end of each voxel layer
      detectBladeCrash:
        type: boolean
      bladeCrashThreshold:
        type: number
        description: should be a number between 0.5 and 1.5
      performDistortionCompensation:
        type: boolean
        description: if true, a predistorted STL file will be created using the distortion simulated by the mechanics solver
      distortionScaleFactor:
        type: number
        description: a value that is used to scale the simulated distortion value
      outputSupportsVtk:
        type: boolean
        description: if true, a VTK file of the support structure will be created
      detectSupportFailure:
        type: boolean
      supportFailureThreshold:
        type: number
        description: should be a number between 0.01 and 1.0
      partFailureThreshold:
        type: number
        description: should be a number between 0.01 and 1.0
      strainWarningThreshold:
        type: number
        description: should be a number between 0.01 and 1.0
      performDistortionCompensationAfterCutoff:
        type: boolean
        description: if true, a predistorted STL file will be created using the distortion after cutoff simulated by the mechanics solver
      distortionAfterCutoffScaleFactor:
        type: number
        description: a value that is used to scale the after cutoff simulated distortion values
      supportType:
        description: type of support used for simulation.
        type: string
        enum:
          - Generated
          - UserDefined

  SingleBeadSimulation:
    allOf:
      - $ref: "#/definitions/Simulation"
      - $ref: "#/definitions/SingleBeadSimulationParameters"

  AssumedStrainSimulationParameters:
    properties:
      layerThickness:
        description: Must be between 0.00001 to 0.0001 meters
        type: number
        minimum: 0.00001
        maximum: 0.0001

  AssumedStrainSimulation:
    allOf:
      - $ref: "#/definitions/Simulation"
      - $ref: "#/definitions/PartBasedSimulationParameters"
      - $ref: "#/definitions/AssumedStrainSimulationParameters"

  ThermalSimulationParameters:
    required:
      - layerThickness
      - hatchSpacing
      - laserWattage
      - scanSpeed
      - slicingStripeWidth
      - startingLayerAngle
      - layerRotationAngle
      - outputStateMap
      - outputShrinkage
      - outputPointThermalHistory
      - includeStressAnalysis
      - anisotropicStrainCoefficientsParallel
      - anisotropicStrainCoefficientsPerpendicular
      - anisotropicStrainCoefficientsZ
      - heaterTemperature
      - outputInstantDynamicSensor
      - outputPrintRitePcsSensor
      - outputInstantStaticSensor
      - outputPointProbe
      - outputCoaxialAverageSensorData
    properties:
      layerThickness:
        description: Must be between 0.00001 to 0.0001 meters
        type: number
        minimum: 0.00001
        maximum: 0.0001
      hatchSpacing:
        description: Must be between 0.00001 to 0.001 meters
        type: number
        minimum: 0.00001
        maximum: 0.001
      laserWattage:
        description: Must be between 10 to 1000 watts
        type: number
        minimum: 10
        maximum: 1000
      scanSpeed:
        description: Must be between 0.01 to 10 meters/second
        type: number
        minimum: 0.01
        maximum: 10
      slicingStripeWidth:
        description: Must be between 0.001 to 0.1 meters
        type: number
        minimum: 0.001
        maximum: 0.1
      startingLayerAngle:
        description: Must be between 0 to 180 degrees
        type: number
        minimum: 0
        maximum: 180
      layerRotationAngle:
        description: Must be between 0 to 180 degrees
        type: number
        minimum: 0
        maximum: 180
      outputStateMap:
        type: boolean
      outputShrinkage:
        type: boolean
      outputThermalVtk:
        type: boolean
      outputThermalVtkLayers:
        type: string
      outputPointThermalHistory:
        type: boolean
        description: for each slectedPoint, a series of vtk files will output thermal history around that point with a radius of staticVirtualSensorRadius.
      staticVirtualSensorRadius:
        type: number
        description: should be a number between 0.05 and 5.0 mm
      includeStressAnalysis:
        type: boolean
        description: false indicates that only the thermal solver will run, while true indicates that the mechanics solver will run after the thermal solver
      anisotropicStrainCoefficientsParallel:
        type: number
      anisotropicStrainCoefficientsPerpendicular:
        type: number
      anisotropicStrainCoefficientsZ:
        type: number
      selectedPoints:
        description: List of points where the thermal solver will collect thermal history - limit 10
        type: array
        items:
          $ref: "#/definitions/SelectedPoint"
      heaterTemperature:
        description: heater temperature in degrees kelvin
        type: number
        minimum: 0
        maximum: 1000
        format: double
      outputInstantDynamicSensor:
        type: boolean
        description: if true, dyanmic sensor data will be collected for each layer specified in the instantDynamicSensorLayers property
      instantDynamicSensorRadius:
        description: radius for instant dynamic sensor data collection in mm
        type: number
        minimum: 0.05
        maximum: 1.5
        format: double
      instantDynamicSensorLayers:
        description: Array of integer layer numbers where instant dynamic sensor data will be collected
        type: array
        items:
          type: integer
          format: int32
      outputPrintRitePcsSensor:
        type: boolean
        description: if true, PrintRite sensor data will be collected for each selectedPoint property
      printRitePcsSensorRadius:
        description: radius for PrintRite sensor data collection in mm
        type: number
        minimum: 0.05
        maximum: 1.5
        format: double
      pyroVirtualSensorOutputAllLayers:
        type: boolean
        description: if true, pyrometer sensor data will be collected for every layer
      outputInstantStaticSensor:
        type: boolean
        description: if true, instant static sensor data will be collected for each selectedPoint property
      instantStaticSensorRadius:
        description: radius for instant static sensor data collection in mm
        type: number
        minimum: 0.05
        maximum: 1.5
        format: double
      outputPointProbe:
        type: boolean
        description: if true, probe sensor data will be collected for each selectedPoint property
      outputCoaxialAverageSensorData:
        type: boolean
        description: if true, coaxial average sensor data will be collected for the deposity layers specified by coaxialAverageSensorZHeights
      coaxialAverageSensorRadius:
        type: number
        description: should be a number between 0.05 and 1.5 mm
      coaxialAverageSensorZHeights:
        description: List of z height (mm) ranges within the part where the coaxial average sensor will collect data
        type: array
        items:
          $ref: "#/definitions/ZHeightRange"

  ThermalSimulation:
    allOf:
      - $ref: "#/definitions/Simulation"
      - $ref: "#/definitions/PartBasedSimulationParameters"
      - $ref: "#/definitions/ThermalSimulationParameters"

  ScanPatternSimulationParameters:
    required:
      - layerThickness
      - startingLayerAngle
      - layerRotationAngle
      - outputShrinkage
      - anisotropicStrainCoefficientsParallel
      - anisotropicStrainCoefficientsPerpendicular
      - anisotropicStrainCoefficientsZ
    properties:
      layerThickness:
        description: Must be between 0.00001 to 0.0001 meters
        type: number
        minimum: 0.00001
        maximum: 0.0001
      startingLayerAngle:
        description: Must be between 0 to 180 degrees
        type: number
        minimum: 0
        maximum: 180
      layerRotationAngle:
        description: Must be between 0 to 180 degrees
        type: number
        minimum: 0
        maximum: 180
      outputShrinkage:
        type: boolean
      anisotropicStrainCoefficientsParallel:
        type: number
      anisotropicStrainCoefficientsPerpendicular:
        type: number
      anisotropicStrainCoefficientsZ:
        type: number

  ScanPatternSimulation:
    allOf:
      - $ref: "#/definitions/Simulation"
      - $ref: "#/definitions/PartBasedSimulationParameters"
      - $ref: "#/definitions/ScanPatternSimulationParameters"

  Part:
    type: object
    required:
      - id
      - organizationId
      - name
    properties:
      id:
        type: integer
        format: int32
        description: Id of the part
      organizationId:
        type: integer
        format: int32
        description: Id of the organization that owns this part record
      name:
        type: string
      originalFileName:
        type: string
        description: The original file name from the user
      description:
        type: string
      tags:
        type: array
        items:
          type: string
      fileBucket:
        type: string
        description: S3 bucket containing part
      fileLocation:
        type: string
        description:  File name of part relative to fileBucket
      availability:
        description: Describes the availabity of the part. Uploaded - the part has been uploaded.  Processing - the part
          is being processed.  Available - the part was processed successfully and can be used in simulations.  Error -
          an error occurred, contact support@3dsim.com.
        type: string
        enum:
          - Uploaded
          - Processing
          - Available
          - Error
      archived:
        description: Designates whether this entity has been archived.
        type: boolean
      xMin:
        type: number
        description: x location (in meters) where the lower left corner of the bounding box of the part will be placed on the bed, calculated when the part is processed, use 0 for initial post
      yMin:
        type: number
        description: y location (in meters) where the lower left corner of the bounding box of the part will be placed on the bed, calculated when the part is processed, use 0 for initial post
      zMin:
        type: number
        description: z location (in meters) where the lower left corner of the bounding box of the part will be placed on the bed, calculated when the part is processed, use 0 for initial post
      sizeX:
        type: number
        description: Width of the Part bounding box (in meters), calculated when the part is processed, use 0 for initial post
      sizeY:
        type: number
        description: Length of the Part bounding box (in meters), calculated when the part is processed, use 0 for initial post
      sizeZ:
        type: number
        description: Height of the Part bounding box (in meters), calculated when the part is processed, use 0 for initial post
      originalX:
        type: number
        description: The smallest x location before transformation
      originalY:
        type: number
        description: The smallest y location before transformation
      originalZ:
        type: number
        description: The smallest z location before transformation
      triangleCount:
        type: integer
        format: int32
        description: Number of triangles in the original STL file, calculated when the part is processed, use 0 for initial post
      volume:
        type: number
        description: Approximate volume of the Part (in meters^3), calculated when the part is processed, use 0 for initial post
      created:
        type: string
        format: date-time
        description: created time stamp, set server-side, read only
      createdBy:
        type: string
        description: assigned user, set server-side, read only
      lastModified:
        type: string
        format: date-time
        description: last modified time stamp, set server-side, read only
      lastModifiedBy:
        type: string
        description: assigned user, set server-side, read only
      supportVolume:
        type: number
      elements:
        type: integer
        format: int32
      nodes:
        type: integer
        format: int32
      collapseCost:
        type: number
      surfaceArea:
        type: number
      estimatedMemory:
        type: integer
        format: int32
        description: Estimated memory usage for mechanics solver using estimateVoxelSize
      maxMemory:
        type: integer
        format: int32
        description: Estimated max memory usage for mechanics solver using estimateVoxelSize
      estimatedWork:
        type: integer
        format: int32
        description: Estimated work using estimateVoxelSize
      estimateVoxelSize:
        type: number
        description: Voxel size used for estimatedMemory, maxMemory, estimatedWork
      errorReason:
        type: string
        description: If an error ocurrs uploading a part, a string will be populated in this field.
      partSupports:
        description: List of supports that have been uploaded for this part
        type: array
        items:
          $ref: "#/definitions/PartSupport"

  PartPost:
    type: object
    required:
      - organizationId
      - name
      - fileLocation
    properties:
      organizationId:
        type: integer
        format: int32
        description: Id of the organization that owns this part record
      name:
        description: Name of the part, max 50 characters
        type: string
      description:
        description: Free form description of the part, max 2048 characters
        type: string
      tags:
        type: array
        description: Tags can be used for any purpose. Max length of serialized json is 256 characters.
        items:
          type: string
      fileLocation:
        type: string
        description: Remote s3 key of a newly uploaded stl file.  Use parts/geometryurl endpoint to upload the part before calling POST.  Max length 256 characters.

  Material:
    type: object
    required:
      - id
      - isCore
      - organizationId
      - name
      - archived
      - key
      - configurationId
      - created
      - createdBy
      - lastModified
      - lastModifiedBy
    properties:
      id:
        type: integer
        format: int32
        description: item identifier
      isCore:
        type: boolean
        description: flag whether it is a core material or a custom material
      organizationId:
        type: integer
        format: int32
        description: organization identifier
      name:
        type: string
        description: material name
        maxLength: 128
      description:
        type: string
        description: material description
        maxLength: 2048
      archived:
        type: boolean
        description: false if not archived, true if archived
      key:
        type: string
        description: key associated with this material.  e.g. Ti64
        maxLength: 16
      configurationId:
        type: integer
        format: int32
        description: identifier for the active configuration for this material
      configuration:
        $ref: "#/definitions/MaterialConfiguration"
      configurationHistory:
        type: array
        items:
          $ref: '#/definitions/MaterialConfiguration'
      created:
        type: string
        format: date-time
        description: created time stamp, set server-side, read only field
      createdBy:
        type: string
        description: creating user, set server-side, read only field
      lastModified:
        type: string
        format: date-time
        description: last modified time stamp, set server-side, read only field
      lastModifiedBy:
        type: string
        description: modifying user, set server-side, read only field
      thermalSimulationApproved:
        type: boolean
        description: true if the material properites have been tuned for thermal simulations

  MaterialConfiguration:
    type: object
    required:
      - materialId
      - created
      - createdBy
      - extinctionCoefOfPowderLaserSpeedCoef
      - extinctionCoefOfPowderLaserPowerCoef
      - extinctionCoefOfPowderOffset
      - extinctionCoefOfSolidLaserSpeedCoef
      - extinctionCoefOfSolidLaserPowerCoef
      - extinctionCoefOfSolidOffset
      - powderToSolidThermalConductivityRatio
      - powderToSolidSpecificHeatRatio
      - powderToSolidDensityRatio
      - solidThermalConductivityAtRoomTemperature
      - solidDensityAtRoomTemperature
      - solidSpecificHeatAtRoomTemperature
      - vaporizationLatentHeat
      - fusionLatentHeat
      - purgingGasConvectionCoefficient
      - solidusTemperature
      - liquidusTemperature
      - vaporizationTemperature
      - vaporizationStartTemperature
      - solidStateTransitionTemperature
      - energyAbsorbingRateByPowder
      - energyAbsorbingRateBySolid
      - thermalExpansionCoefficient
      - elasticModulus
      - elasticModulusOfBase
      - poissonRatio
      - materialYieldStrength
      - supportYieldStrengthRatio
      - materialStrainSensitivity
      - surfaceTension
      - atomicWeight
      - assumedStrain
      - strainPlasticityRelaxationFactor
      - anisotropicStrainCoefficientParallel
      - anisotropicStrainCoefficientPerpendicular
      - anisotropicStrainCoefficientZ
      - capTemperatureLaserSpeedCoef
      - capTemperatureLaserPowerCoef
      - capTemperatureOffset
      - laserSpeedLowerBound
      - laserSpeedUpperBound
      - laserPowerLowerBound
      - laserPowerUpperBound
    properties:
      id:
        type: integer
        format: int32
        description: material configuration identifier
      materialId:
        type: integer
        format: int32
        description: material identifier for this material configuration
      created:
        type: string
        format: date-time
        description: created time stamp, set server-side, read only field
      createdBy:
        type: string
        description: creating user, set server-side, read only field
      extinctionCoefOfPowderLaserSpeedCoef:
        type: number
        format: double
      extinctionCoefOfPowderLaserPowerCoef:
        type: number
        format: double
      extinctionCoefOfPowderOffset:
        type: number
        format: double
      extinctionCoefOfSolidLaserSpeedCoef:
        type: number
        format: double
      extinctionCoefOfSolidLaserPowerCoef:
        type: number
        format: double
      extinctionCoefOfSolidOffset:
        type: number
        format: double
      powderToSolidThermalConductivityRatio:
        type: number
        format: double
      powderToSolidSpecificHeatRatio:
        type: number
        format: double
      powderToSolidDensityRatio:
        type: number
        format: double
      solidThermalConductivityAtRoomTemperature:
        type: number
        format: double
      solidDensityAtRoomTemperature:
        type: number
        format: double
      solidSpecificHeatAtRoomTemperature:
        type: number
        format: double
      vaporizationLatentHeat:
        type: number
        format: double
      fusionLatentHeat:
        type: number
        format: double
      purgingGasConvectionCoefficient:
        type: number
        format: double
      solidusTemperature:
        type: number
        format: double
      liquidusTemperature:
        type: number
        format: double
      vaporizationTemperature:
        type: number
        format: double
      vaporizationStartTemperature:
        type: number
        format: double
      solidStateTransitionTemperature:
        type: number
        format: double
      energyAbsorbingRateByPowder:
        type: number
        format: double
      energyAbsorbingRateBySolid:
        type: number
        format: double
      thermalExpansionCoefficient:
        type: number
        format: double
      elasticModulus:
        type: number
        format: double
      elasticModulusOfBase:
        type: number
        format: double
      poissonRatio:
        type: number
        format: double
      materialYieldStrength:
        type: number
        format: double
      supportYieldStrengthRatio:
        type: number
        format: double
      materialStrainSensitivity:
        type: number
        format: double
      surfaceTension:
        type: number
        format: double
      atomicWeight:
        type: number
        format: double
      assumedStrain:
        type: number
        format: double
      strainPlasticityRelaxationFactor:
        type: number
        format: double
      anisotropicStrainCoefficientParallel:
        type: number
        format: double
      anisotropicStrainCoefficientPerpendicular:
        type: number
        format: double
      anisotropicStrainCoefficientZ:
        type: number
        format: double
      lookupFileLocation:
        type: string
        description: Location where the lookup file is stored in S3.  Set server side.  Only used internally within 3DSIM.
      capTemperatureLaserSpeedCoef:
        type: number
        format: double
      capTemperatureLaserPowerCoef:
        type: number
        format: double
      capTemperatureOffset:
        type: number
        format: double
      laserSpeedLowerBound:
        type: number
        format: double
      laserSpeedUpperBound:
        type: number
        format: double
      laserPowerLowerBound:
        type: number
        format: double
      laserPowerUpperBound:
        type: number
        format: double

  MachineConfiguration:
    type: object
    required:
      - machineId
      - created
      - createdBy
      - hatchSpacing
      - beamDiameter
      - laserWattage
      - laserWaveLength
      - scanSpeed
      - timeDiscretizationDuringScanning
      - coolingToScanningTimeIncrementRatio
      - totalCoolingTime
      - heaterTemperature
      - layerDelay
      - scanLineDelay
    properties:
      id:
        type: integer
        format: int32
        description: machine configuration identifier
      machineId:
        type: integer
        format: int32
        description: machine identifier for this machine configuration
      created:
        type: string
        format: date-time
        description: created time stamp, set server-side, read only field
      createdBy:
        type: string
        description: creating user, set server-side, read only field
      hatchSpacing:
        description: distance between hatch lines in meters
        type: number
        minimum: 0.00001
        maximum: 0.002
        format: double
      beamDiameter:
        description: diameter of laser beam in meters
        type: number
        minimum: 0
        maximum: 0.0001
        format: double
      laserWattage:
        description: laser power in watts
        type: number
        minimum: 0
        maximum: 1000
        format: double
      laserWaveLength:
        description: laser light wavelength in meters
        type: number
        minimum: 0
        maximum: 0.00001
        format: double
      scanSpeed:
        description: speed at which laser travels in meters per second
        type: number
        minimum: 0.01
        maximum: 10
        format: double
      timeDiscretizationDuringScanning:
        description: calculation time interval in seconds
        type: number
        minimum: 0
        maximum: 10
        format: double
      coolingToScanningTimeIncrementRatio:
        description: ratio between cooling to scanning time
        type: number
        minimum: 0
        maximum: 100
        format: double
      totalCoolingTime:
        description: cooling time prior to deposition of next powder layer in seconds
        type: number
        minimum: 0
        maximum: 10
        format: double
      heaterTemperature:
        description: heater temperature in degrees kelvin
        type: number
        minimum: 0
        maximum: 1000
        format: double
      layerDelay:
        description: delay time between layers, stored in seconds
        type: number
        minimum: 0
        maximum: 60
        format: double
      scanLineDelay:
        description: delay time between scan lines, stored in miliseconds
        type: number
        minimum: 0
        maximum: 100
        format: double

  Machine:
    type: object
    required:
      - id
      - isCore
      - organizationId
      - name
      - archived
      - key
      - configurationId
      - created
      - createdBy
      - lastModified
      - lastModifiedBy
    properties:
      id:
        type: integer
        format: int32
        description: machine identifier
      isCore:
        type: boolean
        description: flag whether it is a core machine or a custom machine
      organizationId:
        type: integer
        format: int32
        description: organization identifier
      name:
        type: string
        description: machine name
        maxLength: 128
      description:
        type: string
        description: machine description
        maxLength: 2048
      archived:
        type: boolean
        description: false if not archived, true if archived
      key:
        type: string
        description: key associated with this machine
        maxLength: 16
      configurationId:
        type: integer
        format: int32
        description: identifier for the active configuration for the machine
      configuration:
        $ref: "#/definitions/MachineConfiguration"
      configurationHistory:
        type: array
        items:
          $ref: '#/definitions/MachineConfiguration'
      created:
        type: string
        format: date-time
        description: created time stamp, set server-side, read only field
      createdBy:
        type: string
        description: creating user, set server-side, read only field
      lastModified:
        type: string
        format: date-time
        description: last modified time stamp, set server-side, read only field
      lastModifiedBy:
        type: string
        description: modifying user, set server-side, read only field

  PartUploadRequest:
    properties:
      signedUrl:
        type: string
      s3Key:
        type: string

  PartGeometryUrl:
    properties:
      signedUrl:
        type: string

  Triangle:
    properties:
      normal:
        type: object
        properties:
          angle:
            type: number
            format: double
          x:
            type: number
            format: double
          'y':
            type: number
            format: double
          z:
            type: number
            format: double
      vertices:
        type: array
        items:
          $ref: '#/definitions/Vertex'

  Vertex:
    properties:
      x:
        type: number
        format: double
      'y':
        type: number
        format: double
      z:
        type: number
        format: double

  Error:
    required:
      - code
      - message
    properties:
      code:
        type: integer
        format: int32
      message:
        type: string

  PatchDocument:
    description: A JSONPatch document as defined by RFC 6902
    required:
     - "op"
     - "path"
    properties:
     op:
      type: string
      description: The operation to be performed
      enum:
       - "add"
       - "remove"
       - "replace"
       - "move"
       - "copy"
       - "test"
     path:
      type: string
      description: A JSON-Pointer
     value:
      type: object
      description: The value to be used within the operations.
     from:
      type: string
      description: A string containing a JSON Pointer value.

  SimulationEstimate:
    description: contains an estimate (in minutes) for running an assumed strain simulation
    required:
      - partId
      - estimate
      - type
    properties:
      partId:
        type: number
      estimate:
        type: number
      type:
        type: string
      description:
        type: string
        description: If the estimate is 0, the description field will contain information on why an estimate could not be calculated

  SelectedPoint:
    description: describes a point
    required:
      - label
      - simulationId
      - x
      - 'y'
      - z
      - sensorType
    properties:
      id:
        type: integer
        format: int32
      simulationId:
        type: integer
        format: int32
      label:
        type: string
      x:
        type: number
      'y':
        type: number
      z:
        type: number
      zMin:
        type: number
      zMax:
        type: number
      sensorType:
        description: type of point, usually relates to sensor that will be using this point
        type: string
        enum:
          - PrintRite

  BuildFile:
    description: parameters associated with a build file used by machine manufacturers
    required:
      - id
      - name
      - uploadFileLocation
      - archived
      - availability
      - sizeX
      - sizeY
      - sizeZ
      - machineType
    properties:
      id:
        type: integer
        format: int32
        description: internally assigned identifier for this build file
      organizationId:
        type: integer
        format: int32
        description: identifier for organization this build file belongs to
      name:
        type: string
        description: a name given to this build file
      description:
        type: string
        description: a description of this build file
      tags:
        type: array
        items:
          type: string
        description: a comma delimited list of tags assigned to this build file
      buildFileLocation:
        type: string
        description: folder containing build file contents, multiple files may be contained within the folder
      stlFileLocation:
        type: string
        description: path to STL file describing the part geometry
      sliceZipFileLocation:
        type: string
        description: path to ZIP file containing scan pattern information for each powder deposition layer
      archived:
        type: boolean
        description: true if this build file has been archived, false otherwise
      availability:
        description: Describes the availabity of the build file. Uploaded - the build file has been uploaded.
          Processing - the build file is being processed.  Available - the build file was processed successfully
          and can be used in simulations.  Error - an error occurred and the build file is not available for simulations.
        type: string
        enum:
          - Uploaded
          - Processing
          - Available
          - Error
      sizeX:
        type: number
        format: double
        description: maximum part dimension in X direction
      sizeY:
        type: number
        format: double
        description: maximum part dimension in Y direction
      sizeZ:
        type: number
        format: double
        description: maximum part dimension in Z direction
      createdAt:
        type: string
        format: date-time
        description: time stamp assigned when this build file is created
      createdBy:
        type: string
        description: name of user or entity that created this build file
      lastModifiedAt:
        type: string
        format: date-time
        description: time stamp of last change made to this build file
      lastModifiedBy:
        type: string
        description: name of user or entity that last changed this build file
      fileVersion:
        type: string
        description: version of this build file
      fileCreatedDate:
        x-nullable: true
        type: string
        format: date-time
        description: creation time stamp from original build file
      errorReason:
        type: string
        description: reason build file failed to be processed, if any
      machineType:
        description: type of machine associated with this build file
        type: string
        enum:
          - AdditiveIndustries
          - Renishaw
          - SLM
          - ThreeDSystems
      materialType:
        type: string
        description: material to use when performing simulation
      jobId:
        type: string
        description: job number associated with this build file
      jobName:
        type: string
        description: job name associated with this build file
      numberOfLayers:
        type: integer
        format: int32
        description: number of powder deposition layers
      totalScanDistance:
        type: number
        format: double
        description: total distance laser travels when building the part
      layerThickness:
        type: number
        format: double
        description: thickness of powder deposition layer in meters
      originalX:
        type: number
        format: double
        description: x position of original part location in millimeters
      originalY:
        type: number
        format: double
        description: y position of original part location in millimeters
      originalZ:
        type: number
        format: double
        description: z position of original part location in millimeters
      supportModelFileLocation:
        type: string
        description: path to support model file relative to buildFileLocation
      uploadFileLocation:
        type: string
        description: path to uploaded zip file containing build file components, relative to s3 part bucket

  BuildFilePost:
    description: container for parameters used when posting a new build file
    required:
      - organizationId
      - name
      - fileUploadLocation
      - machineType
    properties:
      organizationId:
        type: integer
        format: int32
        description: identifier for organization this build file belongs to
      name:
        type: string
        description: a name given to this build file
      description:
        type: string
        description: a description of this build file
      tags:
        type: array
        items:
          type: string
        description: a list of tags assigned to this build file
      fileUploadLocation:
        type: string
        description: path of ZIP file containing build file elements
      machineType:
        type: string
        description: type of machine associated with this build file, see BuildFile definition for valid values

  ZHeightRange:
    description: upper and lower bounds for z dimension of an area of interest
    required:
      - zHeightLowerBound
      - zHeightUpperBound
    properties:
      zHeightLowerBound:
        type: number
        format: float32
        description: lower bound of area of interest for z dimension in meters
      zHeightUpperBound:
        type: number
        format: float32
        description: upper bound of area of interest for z dimension in meters

  PartSupportPost:
    type: object
    required:
      - partId
      - name
      - fileLocation
      - originalFileName
    properties:
      partId:
        type: integer
        format: int32
        description: Id of the part that owns this support record
      name:
        description: Name of the support, max 50 characters
        type: string
      description:
        description: Free form description of the support, max 2048 characters
        type: string
      originalFileName:
        description: Original support file name, max 248 characters
        type: string
      fileLocation:
        type: string
        description: Remote s3 key of a newly uploaded stl file.  Use parts/:partId/supports/geometryurl endpoint to upload the support before calling POST.  Max length 256 characters.

  PartSupport:
    type: object
    required:
      - id
      - partId
      - name
      - fileLocation
      - archived
      - availability
      - supportType

    properties:
      id:
        type: integer
        format: int32
        description: Id of the part
      partId:
        type: integer
        format: int32
        description: Id of the part that owns this support record
      part:
        $ref: "#/definitions/Part"
      name:
        type: string
      originalFileName:
        type: string
        description: The original file name from the user
      description:
        type: string
      fileLocation:
        type: string
        description:  File name of part relative to fileBucket
      archived:
        description: Designates whether this entity has been archived.
        type: boolean
      availability:
        description: Describes the availabity of the part. Uploaded - the part has been uploaded.  Processing - the part
          is being processed.  Available - the part was processed successfully and can be used in simulations.  Error -
          an error occurred, contact support@3dsim.com.
        type: string
        enum:
          - Uploaded
          - Processing
          - Available
          - Error
      supportType:
        description: type of geometry. Volumeless - thinwall, closed - watertight
        type: string
        enum:
          - ThinWall
          - Watertight
      xMin:
        type: number
        description: x location (in meters) where the lower left corner of the bounding box of the part will be placed on the bed, calculated when the part is processed, use 0 for initial post
      yMin:
        type: number
        description: y location (in meters) where the lower left corner of the bounding box of the part will be placed on the bed, calculated when the part is processed, use 0 for initial post
      zMin:
        type: number
        description: z location (in meters) where the lower left corner of the bounding box of the part will be placed on the bed, calculated when the part is processed, use 0 for initial post
      sizeX:
        type: number
        description: Width of the Part bounding box (in meters), calculated when the part is processed, use 0 for initial post
      sizeY:
        type: number
        description: Length of the Part bounding box (in meters), calculated when the part is processed, use 0 for initial post
      sizeZ:
        type: number
        description: Height of the Part bounding box (in meters), calculated when the part is processed, use 0 for initial post
      triangleCount:
        type: integer
        format: int32
        description: Number of triangles in the original STL file, calculated when the part is processed, use 0 for initial post
      volume:
        type: number
        description: Approximate volume of the Part (in meters^3), calculated when the part is processed, use 0 for initial post
      created:
        type: string
        format: date-time
        description: created time stamp, set server-side, read only
      createdBy:
        type: string
        description: assigned user, set server-side, read only
      lastModified:
        type: string
        format: date-time
        description: last modified time stamp, set server-side, read only
      lastModifiedBy:
        type: string
        description: assigned user, set server-side, read only
      elements:
        type: integer
        format: int32
      nodes:
        type: integer
        format: int32
      collapseCost:
        type: number
      surfaceArea:
        type: number
      estimatedMemory:
        type: integer
        format: int32
        description: Estimated memory usage for mechanics solver using estimateVoxelSize
      maxMemory:
        type: integer
        format: int32
        description: Estimated max memory usage for mechanics solver using estimateVoxelSize
      estimatedWork:
        type: integer
        format: int32
        description: Estimated work using estimateVoxelSize
      estimateVoxelSize:
        type: number
        description: Voxel size used for estimatedMemory, maxMemory, estimatedWork
      minimumSupportHeight:
        type: number
        description: height (mm) that this support will raise the part
      errorReason:
        type: string
        description: If an error ocurrs uploading a part, a string will be populated in this field.

